{
  "name": "notepad-app",
  "version": "1.0.0",
  "description": "Sticky note app for iOS and Android",
  "scripts": {
    "build": "echo 'No build step needed — static HTML/CSS/JS'",
    "dev": "python3 -m http.server 8000 --directory www",
    "sync": "capacitor sync",
    "open:ios": "capacitor open ios",
    "open:android": "capacitor open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/storage": "^6.0.0",
    "@capacitor/haptics": "^6.0.0"
  }
}
